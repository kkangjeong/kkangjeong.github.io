{
    "dependencies": {
        "@narative/gassby-theme-nevola": "^0.18.4",
        "gatsby": "^2.28.0",
        "react": "^17.0.1",
        "react-dom": "^17.0.1"
    },
    "devDependencies": {
        "gh-pages": "^3.1.0"
    },

    "scripts": {
        "deploy": "gatsby build --prefix-paths && gh-pages -d public"
    }
}
